export type Memory = { date: string; excerpt: string; words: number };
export type ReferenceSummary = { name: string; count: number; dates: string[] };
export type WidgetPlacement = "desktop" | "mobile";
